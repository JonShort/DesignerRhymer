{
  "private": true,
  "name": "WhoIsJonGill",
  "description": "Who is Jon Gill App",
  "version": "0.0.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/JonShort/WhoIsJonGill"
  },
  "scripts": {
    "build": "vbuild",
    "clean": "rm -rf dist",
    "deploy": "yarn build && git subtree push --prefix dist origin gh-pages && yarn run clean",
    "dev": "vbuild -d",
    "lint": "vbuild --eslint",
    "test": "echo lol"
  },
  "devDependencies": {
    "vbuild": "^6.0.0"
  },
  "license": "MIT"
}
