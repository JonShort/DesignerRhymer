{
  "private": true,
  "name": "WhoIsJonGill",
  "description": "Who is Jon Gill App",
  "homepage": "https://jonshort.github.io/WhoIsJonGill",
  "version": "0.0.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/JonShort/WhoIsJonGill"
  },
  "scripts": {
    "build": "vbuild",
    "clean": "rm -rf dist",
    "deploy": "yarn run build && gh-pages -d dist && yarn run clean",
    "dev": "vbuild -d",
    "lint": "vbuild --eslint",
    "test": "echo lol"
  },
  "devDependencies": {
    "gh-pages": "^0.12.0",
    "vbuild": "^6.0.0"
  },
  "license": "MIT"
}
